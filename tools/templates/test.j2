package {{ package_name }}

import (
	"log"
	"testing"

	"github.com/riccardotornesello/irapi-go/testutils"
)

func Test{{ method_name }}(t *testing.T) {
	apiClient := testutils.GetApiClient()

	api := &{{ client_struct_name }}{
		Client: apiClient,
	}
	{% if params_struct %}
	_, err := api.{{ method_name }}({{ params_struct_name }}{}){% else %}
	_, err := api.{{ method_name }}(){% endif %}
	if err != nil {
		t.Fatal(err)
	}

	log.Println("Test{{ method_name }} passed")
}
