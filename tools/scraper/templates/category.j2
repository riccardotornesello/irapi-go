{#
  Template: category.j2
  
  Generates the main.go file for an API category.
  
  This template creates a Go struct that represents an API category
  (e.g., LeagueApi, MemberApi) and includes methods for all endpoints
  within that category.
  
  Variables:
    - package_name: The Go package name (category name)
    - imports: Set of Go package imports needed
    - api_name: Name of the API struct (e.g., "LeagueApi")
    - endpoint_calls: List of rendered endpoint method implementations
#}
package {{package_name}}

import (
	{% for imp in imports %}"{{imp}}"
	{% endfor %}
)

type {{api_name}} struct {
	Client *client.ApiClient
}

{% for endpoint_call in endpoint_calls %}
{{endpoint_call}}
{% endfor %}